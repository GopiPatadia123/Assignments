#include<stdio.h>
main()
{
	int n,rev=0,a,b;
	
	printf("Enter Number : ");
	scanf("%d",&n);
	b=n;	//store the original number
	
	//reverse the number 
	
	//check the is palindrom or not
	if(a==rev)
	{
		printf("Number is Paindrom %d",a);
	}
	else 
	{
		printf("Number is not Palindrom %d",a);
	}
}
